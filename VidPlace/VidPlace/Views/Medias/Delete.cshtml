@model VidPlace.Models.Media
@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete Media</h2>
<p class="error">@ViewBag.ErrorMessage</p>
<h3>Are you sure you want to delete his Media record?</h3>

<div>
    <dl>
        <dt>@Html.DisplayNameFor(model => model.name)</dt>
        <dd>@Html.DisplayFor(model => model.name)</dd>

        <dt>@Html.DisplayNameFor(model => model.ReleasedDate)</dt>
        <dd>@Html.DisplayFor(model => model.ReleasedDate)</dd>
    </dl>
</div>

@using (Html.BeginForm())
{
    <div class="form-group">
        <input type="submit" value="Delete" class="btn btn-default" /> | 
        @Html.ActionLink("Back to List", "Index", "Medias")
    </div>
}

